# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HsbcTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod ‘Moya’, ‘13.0.0’ # latest version at the time of writing.
  pod "KRProgressHUD"
  pod 'Kingfisher', '~> 5.0'
  pod 'MaterialComponents/Buttons'
 
  # Pods for HsbcTest

  target 'HsbcTestTests' do
    inherit! :search_paths
    pod 'KIF'
    pod 'Nimble'
    # Pods for testing
  end

  target 'HsbcTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
